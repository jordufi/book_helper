node_modules/
.expo/
dist/
web-build/

# Proyectos nativos generados por `expo prebuild`: se regeneran, no se commitean.
/android
/ios

*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Clave de la service account de Google Cloud para `eas submit` (Play Console API).
google-service-account.json

.DS_Store
